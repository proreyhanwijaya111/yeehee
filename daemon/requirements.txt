# yeehee daemon - lean deps (no streamlit/plotly/anthropic/scipy/statsmodels)
# Total download size: ~80 MB (was ~200 MB before).

# Data
yfinance>=0.2.40
pandas>=2.1.0
numpy>=1.26.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# HTTP client (LLM router) + Supabase
httpx>=0.27.0
supabase>=2.10.0

# Config / utils
python-dotenv>=1.0.0
pydantic>=2.6.0
tenacity>=8.2.0
psutil>=5.9.0

# Execution API (FastAPI server EA polls for orders) — bundled so single-PC
# install (daemon + EA) works out-of-box. Lean: ~15 MB combined. The EA on
# MT5 calls /api/ea/next-signal here every 5s; without these, EA gets nothing.
fastapi>=0.110.0
uvicorn>=0.27.0
